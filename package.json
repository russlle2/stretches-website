{
  "name": "gmf-productions-website",
  "version": "1.0.0",
  "description": "Official GMF Productions website — music, merch, and live integrations",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "node scripts/build-config.js && node scripts/build-products.js && node scripts/regen-mockups.js && node scripts/stripe-sync-products.js && node scripts/refresh-price-map.js && node scripts/build-posts.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "dotenv": "^17.4.2",
    "sharp": "^0.33.5",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "docx": "^9.7.1",
    "netlify-cli": "^19.0.0"
  }
}
